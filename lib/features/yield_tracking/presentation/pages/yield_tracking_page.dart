import 'package:flutter/material.dart';

/// Req 4: Yield return tracking (Due / Fulfilled / Partially / Overdue).
class YieldTrackingPage extends StatelessWidget {
  const YieldTrackingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Yield Return Tracking')),
      body: const Center(child: Text('Yield tracking – Req 4')),
    );
  }
}
