/// App initialization: DB, auth state, sync.
/// Called once before runApp.
Future<void> bootstrap() async {
  // TODO: Initialize secure storage, local DB, sync engine
  await Future<void>.value();
}
